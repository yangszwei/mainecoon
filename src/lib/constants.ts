/** The base path of the application. */
export const basePath = process.env.NEXT_PUBLIC_BASE_PATH || '';

/** The annotations amount after which to use `VectorImageLayer` for performance. */
export const imageLayerThreshold = 10000;
